import {M} from '../../shared/js/materialize.min.js';

document.addEventListener("DOMContentLoaded", () => {

const elem = document.querySelector('.sidenav');
  const instance = M.Sidenav.init(elem, options);

});
